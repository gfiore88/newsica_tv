# Refactor Day - 2026-05-28

## Obiettivo
Riorganizzare progressivamente il codice NewsicaTV con approccio incrementale, senza downtime funzionale:
- ridurre duplicazioni;
- separare responsabilita' (application/service layer);
- ridurre i moduli monolitici;
- aumentare testabilita' e stabilita' runtime.

## Snapshot Codebase (analisi completa)
Rilevazione al 2026-05-28:
- Python files: `96`
- Python LOC totali: `18514`

Hotspot principali per dimensione:
- `src/dashboard.py` (`1655` LOC)
- `src/newsica/audio/playout.py` (`1220` LOC)
- `src/newsica/agents/editorial_director.py` (`1091` LOC)
- `src/overlay_agent.py` (`1049` LOC)
- `src/newsica/agents/shorts_agent.py` (`764` LOC)
- `src/director.py` (`739` LOC)
- `src/chat_agent.py` (`683` LOC)
- `src/newsica/broadcast/director_agent.py` (`645` LOC)
- `src/newsica/utils/social_publisher.py` (`635` LOC)

## Problemi strutturali rilevati
1. Logica applicativa duplicata tra runtime (`preparation_agent.py`) e UI/API (`dashboard.py`).
2. Moduli-feature mescolati alla dashboard monolitica.
3. Layer applicativo shorts non centralizzato (prima del refactor odierno).
4. Alcune utility di dominio erano annidate in moduli di presentazione.

## Regole operative adottate
1. Refactor a piccoli passi con commit separati.
2. Ogni passo con test automatici.
3. Nessuna modifica distruttiva dei comportamenti runtime.
4. Migrazione progressiva verso moduli `newsica.*` riusabili da agenti e dashboard.

## Attivita' eseguite oggi

### Attivita' 1 - Centralizzazione execution shorts pianificati
Commit: `fa65dff`

Intervento:
- introdotto `src/newsica/shorts/plan_executor.py`;
- unificata l'esecuzione item shorts pianificati per:
  - `src/preparation_agent.py`
  - `src/dashboard.py` (API process one item)
- aggiunto test: `src/newsica/tests/test_shorts_plan_executor.py`.

Impatto:
- eliminata duplicazione di pipeline generate/schedule/update status;
- comportamento coerente tra trigger automatico e trigger manuale.

### Attivita' 2 - Estrazione metadata shorts dalla dashboard
Commit: `504fa87`

Intervento:
- introdotto `src/newsica/shorts/metadata_reader.py`:
  - normalizzazione hashtags;
  - normalizzazione stato social;
  - lettura metadata short da DB/sidecar.
- `src/dashboard.py` ora usa il modulo condiviso.
- aggiunto test: `src/newsica/tests/test_shorts_metadata_reader.py`.

Impatto:
- ridotto peso e responsabilita' dirette di `dashboard.py`;
- logica di metadata accessibile e testabile anche da altri moduli.

### Attivita' 3 - Estrazione API Shorts dalla Dashboard monolitica
Commit: `8415ec1`

Intervento:
- introdotto `src/newsica/web/shorts_routes.py` con registrazione centralizzata degli endpoint shorts:
  - `/api/generate_short`
  - `/api/shorts_publish`
  - `/api/shorts_plan_*`
  - `/api/shorts_library`
  - `/api/shorts_video/<path:filename>`
  - `/api/shorts_delete`
- `src/dashboard.py` ora delega la registrazione via `register_shorts_routes(...)`;
- aggiunto `src/newsica/web/__init__.py`;
- aggiunto test route-level: `src/newsica/tests/test_shorts_routes.py`.

Impatto:
- riduzione ulteriore del monolite `dashboard.py`;
- separazione chiara del layer API Shorts;
- endpoint testabili in isolamento via Flask test client.

### Attivita' 4 - Centralizzazione costanti Shorts
Commit: `bb4dd5a`

Intervento:
- introdotto `src/newsica/shorts/constants.py`:
  - `SHORT_MODES`
  - `SHORT_SOCIAL_PLATFORMS`
- aggiornati i consumer principali (`shorts_agent`, `shorts_routes`, `metadata_reader`, `social_publisher`) per usare costanti condivise.

Impatto:
- riduzione literal duplicati;
- minore rischio di drift tra validazione mode, publish social e UI state.

### Attivita' 5 - Estrazione service layer API Shorts
Commit: `623ff63`

Intervento:
- introdotto `src/newsica/shorts/api_service.py` con business logic centralizzata per:
  - validazione e normalizzazione mode short;
  - generazione short (invocazione `ShortsAgent`);
  - publish social single/all con aggiornamento stato DB;
  - listing libreria shorts;
  - delete file/sidecar + cleanup repository.
- `src/newsica/web/shorts_routes.py` ora contiene handler HTTP sottili e delega al service layer.
- aggiunto test dedicato: `src/newsica/tests/test_shorts_api_service.py`.

Impatto:
- separazione netta tra transport layer Flask e logica applicativa shorts;
- maggiore riuso del dominio shorts anche fuori dalla dashboard;
- aumento della testabilita' unitaria della logica API.

### Attivita' 6 - Estrazione routes storico/debug DB dalla dashboard
Commit: `827f3d4`

Intervento:
- introdotto `src/newsica/web/history_routes.py` con registrazione centralizzata endpoint:
  - `/api/db/history`
  - `/api/db/memory`
  - `/api/db/assets`
  - `/api/db/music-rotation`
- migrati nel nuovo modulo anche helper correlati:
  - formatting memoria editoriale;
  - risoluzione titolo tracce storico;
  - decorazione righe broadcast history;
  - parsing runtime music rotation.
- `src/dashboard.py` ora registra `register_history_routes(...)` e rimuove la logica inline.
- aggiunto test dedicato: `src/newsica/tests/test_history_routes.py`.

Impatto:
- riduzione sostanziale del codice monolitico nella dashboard;
- separazione pulita delle route dati/storico;
- endpoint verificabili in isolamento con test Flask + DB temporaneo.

## Validazioni eseguite
Per ogni attivita':
- `py_compile` sui file toccati;
- unit test mirati verdi.

Validazione complessiva dopo i refactor:
- `PYTHONPATH=src venv/bin/python3 -m unittest discover -s src/newsica/tests -p 'test_*.py'`
- risultato: `Ran 106 tests - OK`.

## Backlog Refactor (prossimi step)
1. Completare estrazione moduli Flask dalla dashboard:
   - `system_routes`.
2. Estrarre service layer per social publishing:
   - boundary unico per publish/schedule/manual vs auto.
3. Ridurre complessita' `shorts_agent.py`:
   - split in `content_selector`, `render_pipeline`, `caption_builder`.
4. Ridurre complessita' `director_agent.py`:
   - isolare recovery policy, breaking policy, scheduling policy.
5. Introduzione linting statico e quality gates progressivi:
   - ruff/flake + complexity thresholds per file.
